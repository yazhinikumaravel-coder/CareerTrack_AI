const $=s=>document.querySelector(s);async function post(url,data){const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();if(!r.ok)throw new Error(j.error||'Request failed');return j}
const lf=$('#loginForm');if(lf)lf.addEventListener('submit',async e=>{e.preventDefault();$('#message').textContent='';try{const j=await post('/api/login',{email:$('#email').value,password:$('#password').value});location.href=j.redirect}catch(err){$('#message').textContent=err.message}});
const rf=$('#registerForm');if(rf)rf.addEventListener('submit',async e=>{e.preventDefault();$('#message').textContent='';try{const j=await post('/api/register',{name:$('#name').value,email:$('#email').value,password:$('#password').value});location.href=j.redirect}catch(err){$('#message').textContent=err.message}});
